using Microsoft.AspNetCore.Mvc;

namespace FairwayAPI.Controllers
{
    [Route("api/[controller]")]
    [ApiController]
    public class LeagueController : Controller
    {
       // Add League Game (Adds the game to the league's list of games. Only added if 4 club members are playing
       // Get all league games
       // Generate League Table
       // Generate league Table with parameters (Dates, Game threshold...)
    }
}
