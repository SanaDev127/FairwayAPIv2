using Microsoft.AspNetCore.Mvc;

namespace FairwayAPI.Controllers
{
    [Route("api/[controller]")]
    [ApiController]
    public class MembershipController : Controller
    {
       // Generate Invite
       // Accept invite
       // CreateMembershipRequest (After accepting invite)
       // AcceptMembershipRequest
    }
}
